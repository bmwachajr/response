<%= form_with(model: answer, local: true) do |form| %>
  <% if answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_area :description, id: :answer_description,  class: "form-control"%>
    <%= form.hidden_field :question_id, id: :question_id, value: question_id %>
  </div>

  <div class="actions">
    <%= form.submit :submit, value: "Submit Answer"%>
  </div>
<% end %>
